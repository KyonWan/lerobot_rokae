"""
rokae-specific extensions for lerobot's recording script.

This package contains small plugin-style helpers that hook into
`lerobot.scripts.lerobot_record` without modifying the core library
too much, so that upstream updates remain easy to track.
"""

