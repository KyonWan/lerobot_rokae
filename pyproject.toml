[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lerobot-rokae-dev"
version = "0.1.0"
description = "Workspace metadata for the LeRobot Rokae stack"
requires-python = ">=3.10,<3.11"
license = {text = "Apache-2.0"}
# 本仓库使用 requirements.txt 安装本地 LeRobot/Rokae workspace 成员。
dependencies = []

[tool.setuptools]
packages = []
